interface:
  display_name: "Emeth Discipline: Architecture Memory"
  short_description: "Recall decisions and preserve project context"
  default_prompt: "Use $architecture-memory to retrieve relevant project context and preserve new durable facts or decisions from this work."

policy:
  allow_implicit_invocation: false
