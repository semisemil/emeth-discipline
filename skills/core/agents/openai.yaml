interface:
  display_name: "Core"
  short_description: "Apply Emeth Discipline baseline quality rules"
  default_prompt: "Use $core to apply Emeth Discipline's shared baseline quality rules to this task."

policy:
  allow_implicit_invocation: false
