interface:
  display_name: "Emeth: Rules"
  short_description: "Apply Emeth baseline quality rules"
  default_prompt: "Use $rules to apply Emeth's shared baseline quality rules to this task."

policy:
  allow_implicit_invocation: false
